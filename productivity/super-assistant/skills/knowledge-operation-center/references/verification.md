# 本次验收记录（2026-09-16）

## 环境与通过项

macOS arm64，Obsidian 1.13.7，使用真实桌面 CLI/API：

| 范围 | 结果 |
| --- | --- |
| 应用迁移至系统目录，版本与签名核验 | 通过 |
| PATH / 系统目录探测、显式目录、中文空格路径、无效路径拒绝 | 通过 |
| 错误 Vault 拒绝、安全相对路径检查 | 通过 |
| create / merge：中文、引号、反斜杠、stdin、尾部空行 | 原始正文逐字节一致 |
| 超过 6000 字符的 Unicode 正文创建和更新 | 正文逐字节一致 |
| list / datetime / checkbox / text 属性，模板读取 | 通过 |
| 搜索、出链、反链、移动后引用自动更新 | 通过 |
| 初始 Base 全库 / 待审查 / 已确认 / 最近修改 | 3 / 3 / 0 / 3 |
| 未解析链接 | 0 |
| 相同 3 个来源 ID 二次导入 | 未新增笔记，已有 Markdown 摘要不变 |
| Skill quick_validate、引用路径、Bash 语法、共享 JSON | 通过 |

## 实测修复与边界

修复 Base 空结果、模板路径未配置、错误消息误判、CLI 提前消耗 stdin 等问题。正文改由 CLI eval 读取本次本地临时文件，并核对 SHA-256；减少长文分块的大量 CLI 调用。单次 CLI 最多等待 25 秒，超时不自动重做可能已发生的写入。升级 Obsidian 后须重新验证，不将本次结果视为未来版本保证。

当前 Skill 已移除下载安装流程，仅保留安装后的初始化。macOS 应用已移至系统目录并重新打开，GUI 确认 Base 显示 3 个结果。Windows 提供常用目录探测和显式路径配置，**尚未进行 Windows 实机运行验收**。

三篇业务资料保持待审查；源码与原文引用日志未取得，知识导入验收不代表业务规则或运行证据已确认。
